services:
  tensorflow:
    image: tensorflow:2.10.1-gpu
    build:
      context: ..
      dockerfile: tensorflow-2.12.0-gpu.Dockerfile
      args:
        USERNAME: kstef
      tags:
        - "tensorflow:2.10.1-gpu"
    container_name: tf-dual-gpu
    user: root
    tty: true
    stdin_open: true
    volumes:
      - /home/kstef/Semantic-Segmentation-Cityscapes:/root/Semantic-Segmentation-Cityscapes
      - /home/kstef/.keras:/root/.keras
      - /home/kstef/dataset:/root/dataset
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0', '1']
              capabilities: [gpu]